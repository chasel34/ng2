/**
 * `core/local` —— 本地纯算法:骰子(共享随机流)、匿名 ID 还原(跳 hex[5])、
 * 彩色标题 TLV、附件目录(固定 UTC+8)等逆向产物。
 *
 * 铁律:本包**禁止 import android.\***;这些算法的怪癖是 NGA 的实现事实,**不许「修正」**。
 * 票 10 的地盘。
 */
package com.chasel.ng2n.core.local
