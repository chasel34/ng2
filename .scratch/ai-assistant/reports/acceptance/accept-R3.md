# 回归抽查 R3（fix-R2 构建，三项定点复验）

环境：emulator-5554 / com.chasel.ng2.dev，未重装、未改代码、未 git 操作。NGA 已登录 lemon43。
设置全程未改动，结束时仍为：单次分析额度＝长楼与个人分析、每日上限＝US$3.00。
今日用量由 ≈US$0.5912 增至 ≈US$0.6539（本轮约 US$0.063）。用量明细：模型请求 253 次 / 输入 2657109 tokens / 缓存命中输入 2286592 tokens / 输出（含思考）203216 tokens / 网页 36 次 / 图片输入 61 张次；每日 21%（含未结预留）。
`logcat -b crash` 无 `com.chasel` 记录，`logcat` 无 `AndroidRuntime` 应用异常、无 ANR、无超过 2 分钟不动的卡住。

样本：用户「达芬奇莉莉」（发帖 10263，19 条样本），一次个人分析（含 3 次「继续」）＋ 历史里零样本条目「咕噜噜喵脆角」。

## 抽查结论速览

| # | 项 | 结果 |
|---|---|---|
| 1 | 倾向卡数量胶囊与正文拼接成句 | **通过**，胶囊为「N 条」，无「N 条发言次」 |
| 2 | 「查看依据」补读主楼的类型胶囊 | **通过**，补读主楼标「主楼」 |
| 3 | 零样本 / 读取失败卡片只配「重试」 | **通过**，草稿说明已消失 |
| 4 | 崩溃与卡住 | 无 |

新问题 2 条，均为 P3。

## 新问题

- [P3] 「达到限制」且本轮没有生成文字时，仍显示「已有草稿保留，可继续」，与同屏文案自相矛盾。复现步骤：个人分析「达芬奇莉莉」→ 触发工具次数上限（32 次）或输出上限。结果：同屏依次为「已读取 19 条；本轮尚无已确认处理完成的样本，19 条待确认处理。**已有草稿保留，可继续。**」「达到限制 · 本次分析的工具执行次数已达上限（32 次）」「**本轮没有生成文字**，已读取的资料已保留。继续会复用已读资料和检查点，可能产生新的费用。」——一句说有草稿，另一句说没生成文字。输出上限那轮（12288 token）同样如此。期望：fix-R2 只给 `READ` / `EMPTY` 两种卡片加了排除条件，`LIMIT` 卡片在「本轮没有生成文字」分支下也不应声称有草稿；判据应改为「本轮是否已写出文字」而不是卡片类型枚举。证据：dump 片段（上引三行同屏）。

- [P3] 样本读取失败多的用户，个人分析要连点 3 次「继续」才出报告，单次额度被吃掉 63%。复现步骤：分析「达芬奇莉莉」（19 条样本，其中 s4/s6/s17 主楼读取失败，多条 `定位楼层` 标 `!`）。结果：第 1 轮 32 次工具调用到顶且零文字；第 2、3 轮各自撞输出上限（12288 token）且零文字，第 3 轮还出现「报告结构尚未完整或来源校验未通过，请继续」；第 4 轮才完成。累计 ≈US$0.0627，本次剩余额度由 US$0.1000 降到 US$0.0374，全程约 7 分钟。期望：票 11 的报告流程应在单次额度内可预期地完成；至少在「零文字 + 反复撞上限」时收敛补读次数，否则额度更低的档位会在拿到报告前先耗尽。证据 `r3-persona-card.png`（完成后的报告）。

## 逐项结论（通过项）

1. **倾向卡数量胶囊 —— 通过**。7 张倾向卡的胶囊全部为「N 条」（5 条 / 8 条 / 3 条 / 2 条 / 1 条），正文读作「涉及影视作品争议与创作者言行的发言有〔5 条〕，多从看乐子、要求创作者自省或补充角色细节的角度切入。」「转述国际时政与社会类新闻的主题有〔8 条〕，覆盖中东、东亚、欧美与东南亚……」——语序完整，未再出现「N 条发言次」。胶囊行内渲染、换行处不裁字，信号条三档（反复出现 / 多次出现 / 偶尔提及）正常。证据 `r3-persona-card.png`。

2. **证据行类型胶囊 —— 通过**。「时政视频博主内容与餐饮安全话题」卡片的「查看依据」抽屉里同屏出现三种胶囊：s13「主题标题」、s30「**主楼**」、s57「回复」。s30 正是模型补读回来的主楼，点进来源预览为「论坛原文 / 30 / 刚刚重新读取 / **0 楼** · 达芬奇莉莉 / 36 字」+「跳到主楼」，与胶囊一致。反向对拍：另一张卡片的 s116 胶囊为「回复」，预览为「1 楼 · 达芬奇莉莉」，判定正确。证据 `r3-persona-drawer.png`。

3. **零样本 / 历史读取失败 —— 通过**。历史页「个人分析 · 咕噜噜喵脆角」（标「读取失败」）重开：范围卡「已读 0 条 · 屏蔽 0 条未计入」，状态卡「历史读取失败 / 内容未读取完成，已保留输入与可用资料，尚未请求模型。」，底部只有「重试」，「估算费用 ≈ US$0.0000」「本次剩余额度 US$0.1000」。accept-R2 记的「已有草稿保留，可继续」已不再出现。证据 `r3-zero-sample.png`。

4. **崩溃与卡住 —— 无**。

## 顺带观察（不计入问题数）

- 本轮首次验到「相反表述 N」按钮（「对京吹三播出后旧作观感受损的惋惜」卡片挂「相反表述 1」），accept-R2 里列为「仍未验证」的那一项已有真实样本；其余倾向卡仍显示「已处理样本中未找到相反表述，不代表不存在。」。
- 预算卡的「N 次请求用量待核实，保守预留未清零，跨日仍占用额度」仍长期挂在设置页与对话页脚（前几轮已记）。
