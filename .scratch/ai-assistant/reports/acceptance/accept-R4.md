# 回归抽查 R4（fix-R3 构建，个人分析单轮完成定点复验）

环境：emulator-5554 / com.chasel.ng2.dev，未重装、未 rebuild、未改代码、未 git 操作。NGA 已登录 lemon43。
设置全程未改动：单次分析额度＝长楼与个人分析（US$0.1000），每日上限＝US$3.00。
今日用量由 ≈US$0.6539 增至 ≈US$0.6848（本轮约 US$0.031），每日 22%（含未结预留）。
`logcat -b crash` 无 `com.chasel` 记录，`logcat` 无应用 `AndroidRuntime` 异常、无 ANR、无超过 2 分钟不动的卡住。

样本与进入路径：accept-R3 同一用户「达芬奇莉莉」（uid 63848616，发帖 10263）。用 `ng2n://read.php?tid=47541576` 打开该用户所发主题，点 0 楼作者名进用户资料页，点「✦ AI 分析发言」。
（该用户名含中文，`adb input text` 无法输入，搜用户走不通；tid 取自设备上 R3 会话记录的样本坐标。）

## 抽查结论速览

| # | 项 | 结果 |
|---|---|---|
| 1 | 一次运行内出报告，最多点 1 次「继续」 | **通过**，点「继续」**0 次**，单轮「已完成」 |
| 2 | 「达到限制」零文字时不写「已有草稿保留」 | **未触发**，本轮没有出现「达到限制」卡，无法在设备上验到 |
| 3 | 崩溃与卡住 | 无 |

新问题 0 条。

## 逐项结论

1. **单轮完成 —— 通过**。10:37:11 点「AI 分析发言」，10:40:42 状态变为「已完成」，全程约 3 分 30 秒，中途只有「正在读取 / 正在思考 / 正在回答，可随时停止」，**从未出现「继续」按钮**，结束后底部是「继续问」与追问输入框。工具调用 **24 次**（上限 32），明细全部为 ✓：1 次「读取技能文档 查看倾向依据 · SKILL.md」＋ 18 次「定位楼层」＋ 5 次「读取主题页」，没有 `!` 失败标记、没有 `limit_reached` 行。汇总行为「已处理 17 条 · 未处理 0 条」，报告含「兴趣与偏好」（含「《吹响！上低音号》/京吹系列动向」等卡片）、「稳定性与变化」（三个时间段）、底部免责与「实际范围」行，并挂有「5 个来源」抽屉与「查看倾向依据 / 查找相反表述 / 查看观点变化」三个快捷操作。
   - 对比 accept-R3：同一用户此前需连点 3 次「继续」、4 轮才出报告、耗 ≈US$0.0627（本次额度的 63%）；本轮 1 轮出报告、耗 **≈US$0.0310**，结束时「本次剩余额度 US$0.0691」（约占单次额度 31%）。
   - 读取失败仍在：报告正文写明「历史读取不完整（TOPICS 第 5 页、REPLIES 第 1 页请求失败），样本不能视为最近完整历史」，与范围卡「历史回复 0 条 / 历史主题 17 个 / 合计 17 / 300 条 / 匿名或无权限 123 条未计入」一致。也就是说在同样的历史分页失败下，本轮没有把工具次数耗光，也没有反复补读。
   - 证据 `r4-persona-report.png`（「已处理 17 条 · 未处理 0 条」＋ 开场说明＋ 首张倾向卡）。

2. **「达到限制」零文字文案 —— 未触发，未验证**。本轮 24 次工具调用未达 32 次上限，也没撞输出上限，界面全程没有出现「达到限制」卡，因此 fix-R3 里 `personaPendingNote` 的分支（零文字时不说「已有草稿保留」）在设备上没有可观察的样本。离线单测 `PersonaContextTest.pendingNoteOnlyClaimsADraftWhenTheRoundActuallyShowedText` 与 `TopicAiSheetTest.limitedRoundWithoutTextDoesNotClaimADraft` 已覆盖；如需真机证据，得找样本更多、补读更重的用户或临时调低上限，本轮按「不为验文案而多烧额度」处理。

3. **崩溃与卡住 —— 无**。`adb logcat -b crash` 中 `com.chasel` 记录 0 条；主 logcat 里的 `AndroidRuntime` 行均来自 `uiautomator` 进程本身。分析期间每 20–25 秒一次 dump，费用与状态持续推进，无 2 分钟静止。

## 顺带观察（不计入问题数）

- 样本组成与 accept-R3 不同：R3 是 19 条（含历史回复），本轮 REPLIES 第 1 页请求失败导致「历史回复 0 条」，样本为 17 条主题标题＋按需补读的 17 个主楼。因此这次是「主楼多为图片、正文可整理内容极少」的读取困难场景，而不是 R3 那种「多条主楼补读报错」的场景；单轮收敛在这次成立，主楼补读连续报错的极端情况本轮未复现。
- 倾向卡数量胶囊仍为「N 条」，正文读作「以主题形式关注京吹系列的发言有〔5 条〕，集中在 2026 年 7 月至 9 月……」，accept-R3 第 1 项结论保持。
- 预算卡的「N 次请求用量待核实，保守预留未清零，跨日仍占用额度」仍挂在设置页与对话页脚（设置页现为 14 次），与前几轮一致。
- 运行中「估算费用」会先涨到 ≈US$0.0457（含预留）再回落到 ≈US$0.0310（结算后释放预留），与「保守预留」说明一致。
