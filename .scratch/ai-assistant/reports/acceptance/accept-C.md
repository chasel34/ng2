# 验收组 C「历史与恢复」（第一轮，主控自测；进程中断恢复待 415 修复后补测）

通过：
- 设置页「AI 对话历史」条数（5 条）、「清空全部对话」文案；历史页分类筛选带计数（全部/主题/楼层/回复链/个人/列表，横向可滚动）；「今天」分组；条目显示来源、时间、阅读范围摘要与状态徽标（已中断）。
- 搜索：≥2 字无结果显示「没有找到相关对话 /「zzq」没有匹配的标题、主题或用户 / 清除搜索」；"iphone" 正确过滤出 3 条。
- 更多菜单：继续聊天 / 打开来源主题 / 删除对话；删除后 Snackbar「已删除对话，用量记录保留」+ 撤销，撤销后条目恢复。
- 主题详情顶部出现「本主题有 1 条 AI 对话」入口条。
- 从历史进入直接全屏，标题栏有「历史」「返回」。

问题：
- [P3] 主题读取失败的对话，历史条目标题与副标题退化为「主题 47552646」并重复：副标题显示「主题 47552646 · 主题 47552646 · 」，末尾多一个分隔符且没有作者信息。复现：在 NGA 读取失败（限流）时发起主题概览 → 历史页。期望：标题缺失时只显示一次 tid 占位，不输出空段与尾部分隔符；若入口页面已知标题（主题详情页已展示标题）应直接使用该标题。证据：history-1.png、history-search-empty.png。
- [P3] 同一对话历史徽标为「已中断」，打开后状态卡是「内容读取失败 / 尚未请求模型」，两处状态口径不一致。期望：票 07 徽标集合（生成中 / 已达额度 / 已中断 / 来源已删除）之外的「读取失败」应映射为更贴切的徽标或文案（例如「未开始」/「读取失败」），或在设计里明确读取失败归入「已中断」。证据：interrupted-conv.png。

待补测（依赖模型请求成功）：杀进程后「上次运行已中断」状态卡与「继续」从检查点恢复；续聊系统规则；存储失败卡。
