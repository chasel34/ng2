# M3 真机验收清单(待验)

- 实现完成:2026-08-09,8 个提交(8bf6ef0 … 09119c0),`pnpm test` 1007 passed / `pnpm typecheck` 全绿
- **前置:必须重新出 EAS development build**——24 的 intent-filter、22 新增的 expo-network / expo-keep-awake 都要进原生包才生效(旧包不崩但不生效)
- 各项细节见对应票的 Comments;此处只列真机才能验的项

## M2 遗留修复复验

- [ ] 精华区/搜索结果标题不再出现 `&#39;`、`&lt;…&gt;`
- [ ] 网事杂谈(fid=-7)版头 0 楼顶部图片正常显示(老域名换 `_ATTACH_BASE_VIEW` 基址)
- [ ] 搜索结果无「帐号权限不足」杂质行;「我的回复/收藏夹」的过期提示行仍在
- [ ] 主楼点赞后退出重进,停 2 秒看计数是否随 refetch 跳成 1(对拍结论:服务端/客户端都正常,疑似旧缓存先上屏)

## 18 反封锁链 + 错误页

- [ ] 「加载失败」错误页与设计稿 1:1;三动作(重试/用网页版打开/重新登录)可用
- [ ] 诊断信息(tid/page/ua/尝试组合)真实落 MMKV,可在设置「导出诊断日志」看到
- [ ] 全链失败耗时体感(6 组合+换账号+Query retry ≈ 14 次快速失败);太慢调 maxAttempts

## 19 Web 反解 + 网页兜底页

- [ ] 数据源提示条与兜底页与设计稿 1:1;「用 APP 阅读这一页」回切后重打原生接口
- [ ] 反解档下「只看某人/只看该楼」表现(fixture 未覆盖)
- [ ] 兜底页 WebView 登录态是原生 cookie 仓库=最后登录账号,多账号时注意
- 已知缺口(不算缺陷):投票模块反解不出、嵌套 from_client 空、匿名楼主翻页丢楼主标记

## 20 帖子缓存

- [ ] 飞行模式打开已缓存主题正常渲染 + 「缓存数据」提示条
- [ ] 清空缓存后设置页占用副标题归零;LRU 淘汰(可临时调小 topic-cache.ts 两个上限常量触发)
- [ ] 长帖「缓存整帖」进度/停止手感;800ms 间隔是否触发风控
- [ ] 我的缓存页与设计稿比对(行布局/清空/删单条)

## 21 屏蔽规则

- [ ] 官方关键词与网页版互通:App 加/删中文词 → 网页版「控制面板→屏蔽」核对;反向再验一次
- [ ] 列表命中隐藏、详情楼层折叠一行灰字、点击展开;「屏蔽此人」落地为本地规则 + 撤销
- [ ] 三 tab 页与设计稿比对(两处有意偏离见票面:对话框「分段+正则勾选」、图标替换)

## 22 设置三屏 + 字号

- [ ] 各开关重启后保持
- [ ] 域名切换后抓包确认请求 host 变化
- [ ] 仅 Wi-Fi 图片:流量下附件折叠(需新 build,expo-network)
- [ ] 手势返回在 Android 上是否真能关;阅读常亮(需新 build,expo-keep-awake)
- [ ] 字号调节实时作用于详情页;重置回当前默认外观
- [ ] 诊断日志分享长文本;「纯白」配色对比度

## 23 签到 + 签名 + 子版块

- [ ] 签到当天第二次点击不发请求(看日志);次日重新发
- [ ] 含 emoji + BBCode 的签名保存后重进显示一致
- [ ] 子版块屏蔽后其主题从版块列表消失;重进版块页 attributes 与本地状态一致(魔法数是试出来的,失效表现在这一步)

## 24 深链

- [ ] 微信/浏览器点 NGA 主题/版块链接弹「用 NGA 阅读器打开」,带 page/pid 落到对应页/楼
- [ ] `adb shell am start -a android.intent.action.VIEW -d "ng2://read.php?tid=45150945&page=2"`
- [ ] 深链冷启动后顶栏返回能退到首页
- [ ] 抽屉「由 URL 读取」合法/非法链接各验一次
