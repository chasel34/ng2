# M1 真机验收记录

- 日期:2026-08-08
- 设备:小米 25113PN0EC(pudding),Android 真机 USB 调试
- 包:EAS development APK(build b2de6870),Metro 经 `adb reverse tcp:8081` 连接
- 验收人:Claude(主控)

## 通过项

| 界面 | 验收点 | 结果 |
| --- | --- | --- |
| 首页 | 墨绿 app bar、分类 tab、公告条(可关闭)、版块宫格 + 远程图标 | ✅ |
| 抽屉 | 未登录头部、「论坛功能」分组 8 个入口 | ✅ |
| 主题列表(fid 650) | 子版 chips、置顶红标题、作者/最后回复/回帖数、[版面镜像] 标记、FAB | ✅ |
| 外链型主题 | 置顶活动帖正确唤起系统浏览器 | ✅ |
| 正文页(tid 45150945 对拍帖) | 楼层卡、[N 楼] + 客户端图标(桌面/iOS/Android)、级别/威望/发帖、引用块(Topic/Reply + uid 链接)、@提及、已编辑标记、点赞条 | ✅ |
| 正文页(真实 57 回复帖) | 热门回复折叠区、长文排版、表情内联混排(NG娘) | ✅ |
| 分页 | 页码按钮切页、跳页对话框(共 N 页提示 + 数字键盘)跳转第 3 页 | ✅ |
| 深浅色 | 系统夜间模式切换即时生效,正文/引用块/首页全部换肤 | ✅ |

## 说明(非缺陷)

- 未登录访客态下 NGA API 本身返回 `"username":"UID:<uid>"`(已用 curl 无 cookie 对拍确认),App 楼层显示 `UID:41417929` 是忠实还原;登录后即显示真实用户名。

## 遗留小缺陷

1. `src/ui/topic-row.tsx:66` 直接渲染 `topic.lastPoster` 原始值,`#ANONYMOUS#` / `#SYSTEM#` 标记未映射为「匿名」/「系统」。建议在 topic-row 或 thread 服务层加显示映射。

## 未覆盖项(依赖特定帖子,已有单测兜底)

- 骰子回放、投票楼层、noimg 补路径:真机未找到现成样例楼层,逻辑由 `src/core` 单测覆盖(560/560 绿)。
