# ng2 — NGA 第三方 Android 客户端

用 Expo 复刻 NGA 论坛的只读向第三方客户端(个人使用),UI 1:1 还原 `design/` 设计稿。本文件只是术语表;范围与技术决策见 `.scratch/app-v1/spec.md` 与 `docs/adr/`。

## Language

### 内容结构

**版块**:
论坛的一个分区,由 `fid` 标识;树状分类组织,可被收藏、搜索、手动按 ID 添加。
_Avoid_: 版面、板块(代码与文档统一写"版块")

**合集**:
一种特殊"版块",由 `stid` 标识,与 `fid` 互斥且优先。
_Avoid_: 专题

**子版块**:
版块下的细分区,可订阅或屏蔽其主题。

**版头**:
版块置顶的头条主题,来自版块数据的 `head` 字段,用普通主题详情打开。

**主题**:
一个讨论串,由 `tid` 标识(真实 tid 需看 `quote_from`)。
_Avoid_: 帖子(歧义:既指主题又指楼层)、话题

**楼层**:
主题内的一条发言,由 `pid` 标识,楼层号 `lou`;第 0 楼称**主楼**。
_Avoid_: 帖子、回复(回复是动作不是实体)

**贴条**:
附着在某楼层下的短评(楼中楼)。本项目只展示、不发送。
_Avoid_: 楼中楼、评论

**热门回复**:
服务端在主楼数据里标记的高赞楼层集合,展示在详情页独立区域。

**回复链**:
从某楼层出发,沿引用关系上下游展开的楼层序列,独立页面可视化。
_Avoid_: 对话链、引用链

**fav 码**:
从主题 `tpcurl` 提取的访问密钥,访问隐藏/过期主题时必带。

### 用户与账号

**账号**:
一对登录凭证(uid + cookie token),通过 WebView 登录获得;可同时登录多个,有且仅有一个**当前账号**。

**匿名还原**:
把 `#anony_<hex>` 形式的匿名作者本地解码为"天干地支+百家姓"六字假名的纯客户端算法。

**签到**:
每日一次的 check_in 操作,客户端按 UTC+8 日期本地去重。

### 消息与通知

**通知**:
回复我/@我/贴条我/获评价等事件的聚合列表;已读状态纯本地维护(服务端不提供)。UI 标签沿用设计稿的「最近被喷」,但代码与文档统一叫通知。
_Avoid_: 提醒、被喷(仅作 UI 文案)

**短消息**:
站内私信会话。本项目不实现(入口保留,点击提示未开放)。
_Avoid_: 私信、短信

### 收藏与过滤

**收藏夹**:
云端的主题收藏容器,支持多夹(新建/重命名/设默认/删除),一个主题可归属多夹。

**版块收藏**:
云端收藏的版块列表(forum_favor2),与主题收藏夹无关。

**屏蔽规则**:
隐藏内容的过滤条目,分**本地规则**(仅本机:用户/关键词/分类,关键词可正则)与**官方屏蔽词**(NGA 账号云端同步的用户与关键词表)。
_Avoid_: 黑名单

### 阅读体验

**阅读进度**:
每个主题记录的"上次读到第 N 楼",再次进入时提示可跳转。

**帖子缓存**:
写入本地的主题页数据,自动(浏览即缓存)+手动(缓存本页/整帖)两种来源;既是离线阅读数据源,也是反封锁链的兜底。

**热帖**:
并发拉取版块前若干页、按 24 小时窗口过滤并按回复数排序的本地聚合列表——不是服务端 API。
_Avoid_: 热门话题(与 NGA 官方叫法混淆)

**彩色标题**:
`titlefont`/`topic_misc` 位掩码驱动的标题样式(颜色/粗斜/下划线)。

**骰子**:
`[dice XdY]` 标签,结果由客户端按 NGA 伪随机算法(种子=authorId+tid+pid)本地复算。

### 网络与容错

**反封锁链**:
读请求失败时的有序兜底策略:格式参数交替 → 换账号重试 → Web 反解 → 帖子缓存 → 网页兜底。

**Web 反解**:
请求同一 URL 的网页 HTML,从内联 JS(`commonui.postArg.proc` 等)反向解出结构化数据的兜底手段。
_Avoid_: HTML 抓取、爬虫模式

**网页兜底**:
反封锁链全部失败后,用内置 WebView 直接展示原网页的最终降级页面。
